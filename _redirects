---
description: generates a netlify _redirects compatible file for serverside shortlinks
---
# redirects from post title to full permalink generated by jekyll
{% for post in site.posts %}/{{post.title|slugify}}  /{{post.date | date: "%Y/%m/%d"}}/{{post.title|slugify}}
{% endfor %}